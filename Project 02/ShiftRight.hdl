/**
*Shifts 16-bits to the right (divides by two and floors)
*/
CHIP ShiftRight{
     IN in[16];
     OUT out[16];
     PARTS:
     Mux16(a[15] = in[15], a[0..14] = in[1..15], b[0..15] = false, sel=false, out=out);
}